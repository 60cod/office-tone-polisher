{
  "name": "office-tone-polisher",
  "version": "1.0.0",
  "description": "Netlify serverless proxy for Gemini API",
  "main": "index.html",
  "scripts": {
    "build": "echo 'No build step needed for static HTML'",
    "start": "netlify dev"
  },
  "dependencies": {
    "@google/generative-ai": "^0.1.0"
  },
  "engines": {
    "node": "18.x"
  }
}